abstract class ListItem {
  final int id;
  final String imageName;
  final String title;
  late final String description;

  ListItem(
      this.id,
      this.imageName,
      this.title,
      );
}